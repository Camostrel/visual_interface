# REQUIREMENTS — требование → как реализовано

Живой чек-лист: для каждого требования — **как именно** оно у нас сделано и **где** в коде.
Обновляется вместе с разработкой. Приоритеты и фазы — в [ROADMAP.md](ROADMAP.md),
общая карта модулей — в [ARCHITECTURE.md](ARCHITECTURE.md).

Статусы: ✅ готово · 🟡 частично · 🔴 нет · ⏸ отложено.

---

## 1. Векторная подложка и движок карты

### 1.1 «Отрисовать 2D-план этажа, только стены» — 🟡

**Как реализовано:** SVG-файл плана лежит в `www/visual_interface/assets/floors/`.
Загружается по формуле: `layout.floors[floorId].svg` → иначе `assets/floors/<floor_id>.svg`
→ иначе fallback `default-floor.svg`. Линии стен рисуются `currentColor` и наследуют цвет темы
(`.arvid-svg-plan { color: var(--svg-plan-line) }`), поэтому план читается и в тёмной, и в светлой.

**Где:** `js/floor-page.js → getFloorSvg()`, `js/svg-utils.js → loadSvgInto()`, `css/room.css` (`.arvid-svg-plan`).

**Что осталось:** сейчас `default-floor.svg` — нарисованный нами тестовый этаж (коридор + кабинеты).
Реальный план объекта (из DWG) — Фаза 3.

### 1.2 «Каждое помещение — замкнутый невидимый полигон (контур)» — ✅

**Как реализовано:** контуры лежат **внутри SVG-файла** — это элементы с классом `room-zone`
и атрибутом `data-room-id="<area_id>"`:

```xml
<g id="room-zones">
  <rect class="room-zone" data-room-id="ofis" x="45" y="415" width="390" height="300"/>
</g>
```

Может быть любой `<path>`/`<polygon>`, не только прямоугольник. Приложение находит их через
`svg.querySelectorAll(".room-zone[data-room-id]")` и навешивает поведение.

> **Важное уточнение (частый вопрос).** Сами **контуры — внутри SVG** (как и было раньше).
> А вот **стилизация вынесена наружу**, в `css/floor.css` (с v0.4.0). Раньше `<style>` с
> `.room-zone` жил внутри каждого SVG-файла — это дублировалось в каждом плане и не знало о теме.
> Теперь SVG отдаёт только геометрию, а внешний вид (прозрачность, hover, подсветка по свету)
> задаёт проект. Поэтому новые планы можно рисовать без стилей — достаточно `class="room-zone"`.

**Где:** SVG-файлы `assets/floors/*.svg`; `js/floor-page.js → bindRoomZones()`; `css/floor.css` (`.room-zone`).

### 1.3 «Механика масштабирования для сенсорных экранов» — ✅

**Как реализовано:** собственный движок `ArvidSvgPanZoom` (без библиотек). Меняет **`viewBox` SVG**,
а не CSS-transform — поэтому координаты устройств остаются в системе плана и не «плывут».

| Жест / элемент | Что делает |
|---|---|
| Колесо мыши | зум к точке курсора |
| Перетаскивание | панорамирование (`panByScreenDelta`) |
| Щипок двумя пальцами (pinch) | зум к центру между пальцами |
| Кнопки `+` / `−` / `⤢` | зум к центру / сброс масштаба |

Ограничения: `minZoom 0.45`, `maxZoom 18`, шаг `1.22`.

**Тап vs перетаскивание (v0.11.1).** Раньше pan **не запускался**, если палец попадал на зону
или маркер — а на объекте зоны покрывают почти весь план, и перетащить его было нельзя. Теперь
pan запускается всегда, а тап и перетаскивание различаются **по сдвигу пальца** (порог 8px):
ушёл дальше — панорама, «случайный» клик по зоне/лампе после неё гасится; не ушёл — тап;
два пальца — зум. `setPointerCapture` убран (мог перенаправить `click` мимо зоны).

**Где:** `js/svg-utils.js → ArvidSvgPanZoom` (`onPointerDown`/`onPointerMove` + капчер-суппрессор
клика, `zoomAt`, `handlePinchZoom`, `bindToolbar`).

---

## 2. Логика карты и переход

### 2.1 «light=on → контур жёлтый/светлый; off → прозрачный» — ✅

**Как реализовано:** не YAML-правилом (мы не Lovelace), а нативно: на каждое `state_changed`
пересчитывается состояние комнат и на зону вешается класс `has-light-on`, который красит её
**прозрачным оранжевым** (цвет согласован, решение D в ROADMAP).

Истина «свет включён» берётся из **HA-группы комнаты** `light.<area_id>` (если есть),
иначе — «горит хотя бы одна лампа помещения».

**Где:** `js/floor-page.js → applyZoneStateClasses()`, `getRoomStats()`; `css/floor.css` (`.room-zone.has-light-on`).
Подсветка по свету видна в режиме карты «Освещение» (по умолчанию) — см. §4.

### 2.2 «Клик по помещению → план отдельной комнаты» — ✅

Реализовано как **двойной тап** (см. 3.2): открывается view комнаты с её планом.

**Где:** `js/floor-page.js → handleZoneTap()` → `openRoomByArea()`; `js/spa-router.js`.

### 2.3 «Светильники на плане комнаты, отличаются от стен» — ✅

**Как реализовано:** устройства не зашиты в SVG — рисуются поверх плана как SVG-маркеры
(иконка-панель с гравировкой, 48×48 в координатах viewBox) в отдельном слое `#arvid-device-markers`.
Координаты берутся из нашего backend-стора (`layout.devices[entity_id] = {x, y, area_id}`).
Включённая лампа подсвечивается (класс `is-on`: свечение/яркость иконки).

**Где:** `js/room-page.js → renderDeviceMarkers()`; `js/device-ui.js → iconAssetUrl()`;
`assets/icons/{light,motion,panel}.svg`; `css/room.css` (`.device-marker`).

### 2.4 «Кнопка "Выключить весь этаж" в фиксированном месте» — ✅ (в правой панели)

**Как реализовано:** это **быстрое действие** в правой панели («Выключить свет этажа»).
Отдельная плавающая кнопка поверх карты была добавлена в v0.5.0 и **удалена в v0.6.0** как
дублирующая (решение пользователя).

Логика (v0.6.0): гасит **HA-группу этажа** `light.<floor_id>` (например `light.3_etazh`).
Если группы нет — фолбэк: собирает лампы комнат этажа и пишет предупреждение в лог.

**Где:** `js/floor-page.js → turnOffFloorLights()`; `index.html` (`[data-floor-lights-off]`).

---

## 3. Прямое управление комнатами (без перехода)

### 3.1 «Короткий тап по полигону → вкл/выкл света помещения» — ✅ (только в режиме «Освещение»)

Переключает **HA-группу комнаты** `light.<area_id>` (детерминированно).
Фолбэк при отсутствии группы — все лампы помещения + предупреждение в лог.

**Ограничение (v0.8.1):** одиночный тап управляет светом **только в режиме «Освещение»**.
В «Присутствии» и «Диагностике» карта информационная — случайный тап не должен гасить свет
в помещении, за которым наблюдают. Двойной тап (переход в комнату) работает во всех режимах.
`Enter`/`Пробел` на зоне: в «Освещении» — свет, иначе — переход в комнату.

**Где:** `js/floor-page.js → handleZoneTap()` → `canToggleLightsFromMap()` → `toggleRoomLights()`.

### 3.2 «Двойной тап → карточка пространства» — ✅

Различение тапов по таймауту **250 мс**: первый тап ставит таймер (по истечении — свет),
второй тап в окне — отменяет таймер и открывает комнату. На зонах стоит
`touch-action: manipulation`, чтобы браузер не зумил страницу по двойному тапу.

**Где:** `js/floor-page.js → handleZoneTap()`; `css/floor.css` (`.room-zone`).

### 3.3 «(Опц.) Светильники на общем плане при зуме» — ✅ (v0.10.0)

**Как реализовано.** Устройства объявляет **сам план** (`data-entity`, ставит
`tools/plan_convert.py`), поэтому они уже есть в DOM вместе с SVG — создавать их не нужно.

| Масштаб | Что видно |
|---|---|
| < 2.2× | помещение показывает **агрегат**: зона красится/пульсирует (166 значков с высоты этажа читались бы как шум) |
| ≥ 2.2× | зона гаснет до контура, солируют **устройства**; проблемные пульсируют красным точечно |

Переключение — класс `is-zoomed` на `.svg-stage`, а не перерисовка: ни создания узлов,
ни пересчёта координат. Порог `ArvidFloorPage.DEVICE_ZOOM_THRESHOLD`.

**Где:** `js/floor-page.js → applyZoomLevel()/collectPlanDevices()/updatePlanDeviceStates()`;
`js/svg-utils.js → onZoom`; `css/floor.css` (`.device-node`, `.is-zoomed`).

**Не сделано:** замер памяти/быстродействия на 166 устройствах (пункт ТЗ); порог 2.2 подобран
на глаз.

### 3.4 Планы из CAD: устройство объявляет себя само (D2, v0.10.0)

Два независимых пути координат — они сосуществуют и не мешают друг другу:

| Путь | Источник координат | Элемент | Для чего |
|---|---|---|---|
| **Расстановка** | наш стор `layout.devices` | `.device-marker` | частные объекты, десятки устройств |
| **План** | сам SVG (`data-entity`) | `.device-node` | продакшн: сотни устройств, чертёж из CAD |

Конвейер: чертёж (AutoCAD → PDF → SVG) → `tools/plan_convert.py` → эталон в `assets/floors/`.
Скрипт переносит имя из текстовой подписи («L 1.12.31») в геометрию светильника
(`data-entity="light.l_1_12_31"`), снимает цвета (красит тема) и чистит CAD-мусор.
Мини-планы комнат режет `tools/plan_rooms.py`. Правила — [SVG_PLAN_SPEC.md](SVG_PLAN_SPEC.md).

Устройство нарисовано на плане, но в HA его нет (план богаче объекта) → класс `is-unknown`,
показываем еле заметным: выдавать за рабочее нельзя.

### 3.5 Полламповое управление на плане комнаты — ✅ (v0.11.1–0.11.2)

Тап по **отдельной лампе** на плане комнаты — и по маркеру расстановки (`.device-marker`), и по
устройству плана из CAD (`.device-node`):

| Жест | Действие |
|---|---|
| Короткий тап по лампе | `light.toggle` **этой** лампы |
| Долгое удержание | попап точечного управления (ползунок яркости + Вкл/Выкл) |
| Тап по датчику/панели | попап показаний |

Единый жест — `bindDevicePress` (переиспользуется маркерами и планом; `blockPan:true` у маркеров,
`false` у устройств плана, чтобы план можно было перетаскивать за лампу). Лампа кликается по **всей
площади полигона** и в погашенном состоянии: `.device-node { fill: transparent }` — заливка невидима,
но остаётся hit-областью (v0.11.2; с `fill:none` ловился только тонкий контур). Незнакомые лампы
(`is-unknown`) некликабельны. На плане ЭТАЖА лампы пока только индикаторы (`pointer-events:none`).

**Где:** `js/room-page.js → bindDevicePress()/bindPlanDeviceEvents()/handleMarkerClick()`;
`css/floor.css` (`.device-node`, `.device-node.is-interactive`).

---

## 4. Режимы карты (селектор «Освещение / Присутствие / Диагностика») — ✅ Фаза 2 (v0.8.0)

**Переключатель:** таб-контрол `.map-mode-switch` — плавающий оверлей **над планом**
(как `.plan-toolbar`), одинаковый на ПК и телефоне. Раньше это был `<select>` в топбаре, но
`.topbar-actions` скрыт в мобильном портрете — режим был недоступен на телефоне (долг D4).
Оверлей не влияет на мобильную сетку: высота плана считается от ширины `.svg-stage`.

| Режим | Требование | Как реализовано |
|---|---|---|
| Освещение | полигоны по статусу `light` | ✅ `.has-light-on` → оранжевая заливка (см. 2.1) |
| Присутствие | зелёный, если есть движение | ✅ `.has-motion` → **зелёный пульс 2.6 с**; свет **игнорируется** |
| Диагностика | карта серая, красным пульсируют offline | ✅ линии плана → `--muted`; `.has-offline` → **красный пульс 1.7 с**; `.has-anomaly` → янтарная заливка |

**Цвета (v0.8.2).** Присутствие пульсирует как диагностика, но **медленнее**: движение — норма,
а не авария. Выбор зоны (hover / `is-active`) — **синий** (`--zone-select`), а не `--accent`:
акцент темы зелёный (#27d09a) и сливался с индикацией движения. На пульсирующих зонах заливку
задаёт анимация, поэтому выбор там показывается синим **контуром**.

**Механика (важно).** JS вешает на зону **все** классы состояния сразу, а видимый слой выбирает
CSS по `data-map-mode` на `.svg-stage`. Поэтому смена режима — один атрибут, без перерисовки, и
инвариант «DOM не перестраивается по `state_changed`» сохраняется.

**Серость карты** сделана цветом линий плана, а не `filter: grayscale()` — фильтр обесцветил бы
и сам красный пульс.

### 4.1 Откуда берётся «Диагностика» — из ядра DALI, не из наших вычислений

Своей логики offline интерфейс **не ведёт**. Данные — от ядра DALI: основной путь — push-подписка
`arvid_dali_center/health_subscribe` (ядро ≥ v1.1.1), фолбэк для старого ядра — поллинг
`health_data`. Обе read-only, без `require_admin` (подробнее — ниже, «Обновление v0.8.2»).

**Полная таблица поведения** (метки — `KIND_LABEL` из `health/evaluator.py`, условия — `_compute()`):

| `kind` ядра | Метка ядра (видна в его карточке) | Условие в ядре | Наш слой | Поведение на карте |
|---|---|---|---|---|
| `lamp_offline` | Лампа: оффлайн | сущность `unavailable` дольше грейса | `offline` | 🔴 красный пульс зоны |
| `lamp_unknown` | Лампа: состояние неизвестно | сущность `unknown` дольше грейса | `offline` | 🔴 красный пульс зоны |
| `sensor_unknown` | Датчик: состояние неизвестно | `unavailable` под грейсом, либо `unknown` дольше `lux_stale_h` (7 ч) | `offline` | 🔴 красный пульс зоны |
| `panel_unknown` | Панель: нет связи | `unavailable` / `unknown` | `offline` | 🔴 красный пульс зоны |
| `motion_stuck` | Движение залипло | «ДВИЖЕНИЕ» дольше `motion_stuck_h` (1 ч) | `anomaly` | 🟠 янтарная заливка зоны |
| `motion_idle` | Свободно слишком долго | «свободно» дольше `clear_h` (7 ч) | `anomaly` | 🟠 янтарная заливка зоны |
| `lux_stale` | Освещённость не меняется | значение не менялось дольше `lux_stale_h` (7 ч) | `anomaly` | 🟠 янтарная заливка зоны |
| `gw_offline` | Шлюз оффлайн | `hub.state` = offline/failed | `gateway` | строка в «Предупреждениях» (к помещению не привязан) |

**Приоритет в зоне:** если в помещении есть хоть один `offline` — зона пульсирует красным,
янтарь не показывается (`has-anomaly` вешается только при `offlineCount === 0`). Логика:
«не на связи» важнее «странно себя ведёт».

**Что это значит на практике** (важно для трактовки):
- `motion_idle` — **не поломка**. Ночью или в выходные пустое помещение честно даёт «свободно
  дольше 7 часов». Поэтому оно янтарное, а не красное: янтарь = «посмотри», красный = «почини».
- `lamp_unknown` ≠ `lamp_offline`: первое — лампа есть, но состояние неизвестно (например, после
  рестарта HA до первого отчёта), второе — связи нет.
- Устройства **без area** (не привязаны к помещению в HA) на карте не отображаются вовсе —
  их ошибки уходят в счётчик «Без помещения» в «Предупреждениях». Это же чинит задача H3.

> **Открытый вопрос (обсудить с заказчиком).** Деление на «красный/янтарный» — наше, ядро отдаёт
> плоский список без severity. Если заказчику нужна другая раскраска (например, `motion_stuck`
> красным, а `motion_idle` вовсе скрыть) — правится в одном месте: `ArvidHealth.KIND_SEVERITY`.

**Обновление (v0.8.2): живая подписка `health_subscribe`** (ядро ≥ v1.1.1), поллинга нет.
Ядро шлёт снимок сразу и заново на каждый свой пересчёт. Старое ядро (`unknown_command`) →
фолбэк на `health_data` по таймеру (300 с; 30 с в «Диагностике»).

Почему не поллинг: `health_data` форсирует **полный пересчёт** здоровья на каждый запрос (обход
всех устройств всех шлюзов, в петле HA). Плюс `gw_offline` появляется через ~1.5 с (грейс 5 минут
на шлюз **не** распространяется) — поллинг задерживал самое срочное событие на срок до 300 с.

**Нет ядра** (`unknown_command`) → модуль отключается, «Предупреждения» показывают
«Данные о здоровье недоступны», Освещение/Присутствие работают как обычно.

**Адресация записей** (ядро v1.1.1): `area_id`, `floor_id`, `entity_id`, `device_id`.
Зоны сопоставляем по `area_id` (фолбэк по имени — для записей из персиста, ≤15 с после рестарта HA).
Маркер устройства — по **`device_id`**: пара движение+люкс = одно устройство, но две записи
здоровья с разными `entity_id`.

**Где:** `index.html` (`[data-map-mode-switch]`), `js/health.js`,
`js/floor-page.js → setMapMode() / applyZoneStateClasses() / renderFloorWarnings()`,
`css/floor.css` (`.map-mode-switch`, `.room-zone.has-*`, `.floor-warning`).

---

## 4.5 Состав комнаты: что откуда берётся (v0.7.0)

Частый вопрос — «почему в комнате N устройств, хотя в HA привязано меньше».

| Что | Источник | Метод |
|---|---|---|
| **Состав комнаты** (карточки, счётчик «N/N включено», статистика) | **только HA**: `entity.area_id` / `device.area_id` | `ARVID_APP.entitiesForArea()` |
| **Маркеры на плане** | наш backend-стор: `layout.devices[id] = {x, y, area_id}` | `ARVID_APP.placedEntitiesForRoom()` |
| Фильтр «свои события» (подписка) | объединение обоих | `ARVID_APP.entitiesForRoom()` |

Если устройство **стоит на плане комнаты, но в HA к ней не привязано** — маркер рисуется
**приглушённым и обесцвеченным**, а в карточках комнаты появляется плашка
«N устр. на плане не привязано к помещению в HA». В состав и в группу света оно не входит.

**Где:** `js/app-state.js → entitiesForArea / placedEntitiesForRoom / isUnassignedInRoom`;
`js/room-page.js → getRoomComposition / getPlacedEntities / buildUnassignedNote`;
`css/room.css` (`.device-marker.is-unassigned`, `.unassigned-note`).

---

## 4.6 Группы света — не устройства (v0.9.0)

**Проблема:** DALI-группа ядра (`DaliGroupLight`) — это сущность `light.*`. Если ей задана area,
она попадала в состав комнаты как обычная лампа, и счётчик «N/N включено» считал лампы дважды —
саму лампу и группу, в которую та входит.

**Признак группы** (`ARVID_APP.isLightGroupState`), по `entity_id` её не отличить (имя произвольное,
например «r2»):

| Вид группы | Признак |
|---|---|
| DALI-группа ядра | `model === "DALI Group"` у **устройства** в реестре HA (`light.py` ядра) |
| Группа/хелпер HA (`light.<area_id>`, `light.<floor_id>`, `light.all`) | список членов в атрибутах: `entity_id: [...]` |

**Группы исключены из скоупа устройств:** не считаются лампами, не показываются в карточках,
в редакторе расстановки и на плане. Управление ими сохранено — селект «Группа света» берёт их
отдельно (`getRoomLightGroups()`).

**Где:** `js/app-state.js → isLightGroupState()`; `js/ha-registry.js → getDeviceModel()`;
`js/room-page.js → isScopedState()/getRoomMemberLights()/getRoomLightGroups()`;
`js/floor-page.js → getRoomMemberLights()/getAllLightEntityIds()`.

## 4.7 Offline в комнате (D6, v0.9.0)

| Что | Поведение |
|---|---|
| Маркер устройства не на связи | приглушён, обесцвечен, красное кольцо |
| Маркер с аномалией | янтарное кольцо (устройство отвечает, данные подозрительные) |
| Карточки | плашка «N устр. не на связи» со списком имён |
| Строка датчика / панели | приглушена, показывает «не на связи» вместо устаревших показаний |
| Управление светом | заблокировано, если **ни одна** лампа комнаты не на связи |

Пульса в комнате нет (в отличие от зоны на этаже): маркеров много, мигание было бы шумом.

> ⚠ Сопоставление — **по `device_id`**, не по `entity_id`: пара движение+люкс это одно устройство
> и один маркер (координата под `ms_`), но две записи здоровья с разными `entity_id`.

**Где:** `js/room-page.js → getHealthFor()/isDeviceOffline()/buildOfflineNote()`; `css/room.css`.

## 5. Управление светом — модель (v0.6.0)

**Принцип: детерминированные HA-группы, а не поиск ламп.**

| Уровень | Сущность-группа | Формула |
|---|---|---|
| Комната | `light.ofis` | `light.<area_id>` (area «Офис» → id `ofis`) |
| Этаж | `light.3_etazh` | `light.<floor_id>` (этаж «3 этаж» → id `3_etazh`) |
| Весь объект | `light.all` | фиксированное имя |

Группы создаются в HA заранее (логические группы из DALI-групп). Если группы нет — код падает
в фолбэк-сборку ламп и пишет `warn` в консоль. Групповая сущность **не считается лампой**
в счётчиках «N/N включено» (исключается по имени).

**Где:** `js/app-state.js → lightGroupState()`; `js/floor-page.js → toggleRoomLights /
turnOffFloorLights / setAllLights`; `js/room-page.js → getLightTargetIds()` («Вся комната»).

---

## 6. Производительность и обновление UI (v0.6.0)

**Требование (из практики):** не перерисовывать интерфейс на каждое изменение состояния —
поток показаний люкс-датчика заставлял кнопки и слайдер мигать.

**Как реализовано:** разделены **render** (построение DOM) и **update** (только значения):

- `handleStateChanged(event)` фильтрует чужие сущности и **коалесцирует** пачку событий
  в один кадр через `requestAnimationFrame`.
- Обновляются только: классы маркеров (`data-entity`), счётчик «N/N включено»
  (`data-light-count`), показания датчиков (`data-sensor-anchor`), события панелей
  (`data-panel-entity`), классы зон и режимов.
- Слайдер яркости **не трогается**, пока пользователь его удерживает или он в фокусе.
- На этаже `bindRoomZones()` больше **не** вызывается по `state_changed` (раньше перевешивал
  все обработчики на каждое событие).

Полная перерисовка — только при смене комнаты/этажа, входе-выходе из режима редактирования
и сохранении разметки.

**Масштаб — индексы реестра (v0.11.0, долг D20).** Резолв `area_id → сущности` и
`device_id → сущности` вынесен в `Map` (было `states.filter` по всему реестру, в цикле — O(N²));
обновление состояния — O(1); этаж фильтрует «свои» события (был глобальный пересчёт на каждое
событие HA); статистика зон считается один раз за кадр (была дважды). Замер: 4400 сущностей,
200 событий × 3 зоны — 301 мс → 5 мс. Состав пересобирается по изменению реестров HA (`addCompositionHandler`).

**Где:** `js/room-page.js → handleStateChanged/scheduleStateUpdate/updateControlValues/updateDeviceMarkerStates`;
`js/floor-page.js → handleStateChanged/refreshFloorState/computeFloorStats`; `js/ha-registry.js` (индексы).

---

## 6.5 Устойчивость связи и сохранность layout (v0.11.0)

| Требование | Как реализовано |
|---|---|
| Обрыв связи не должен выглядеть как рабочий экран | Реконнект WS 1с→30с, восстановление подписок, перечитывание состояний, **плашка «нет связи»** + гашение управления (A3) |
| Смена темы на одном устройстве не должна стирать расстановку с другого | Точечные записи `layout/ui/update` (тема) и `layout/devices/update` (только изменённые); полная запись шлёт `base_rev` → конфликт вместо тихой перезаписи; `asyncio.Lock` в backend (A4) |
| Ошибка маршрута не должна уничтожать вёрстку | Оверлей с кнопкой «Повторить» вместо `innerHTML = "Ошибка…"` (D21) |

**Где:** `js/ha-ws.js`, `js/app-state.js`, `js/shell-ui.js`, `js/spa-router.js`,
`custom_components/visual_interface/{storage,websocket_api}.py`.

---

## 7. Прочее (реализовано, вне чек-листа заказчика)

| Возможность | Где |
|---|---|
| Полламповый тап по лампе на плане комнаты (см. 3.5) | `room-page.js → bindDevicePress` |
| Режим редактирования расстановки в комнате (список + поиск, drag/тап) | `room-page.js` |
| Единый датчик движения+освещённости (пара `ms_`/`il_` по `device_id`) | `room-page.js → collapseToUnits`, `device-ui.js` |
| Свой backend-стор координат (`visual_interface.layout`) | `custom_components/visual_interface/` |
| Тема (тёмная/светлая), сохраняется точечно в layout | `shell-ui.js → setTheme` (`layout/ui/update`) |
| Quick View (мини-карточка комнаты) | ❌ удалён в v0.10.2 (был отключён с v0.5.0). Понадобится — заново на текущем функционале |
