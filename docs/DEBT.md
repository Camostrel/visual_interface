# DEBT — реестр долгов и открытых вопросов

Актуально на **v0.11.6 (2026-07-15)**, задеплоено на HA. Обновлять при закрытии/появлении долгов.
Приоритеты: **P0** молча портит данные или врёт пользователю · **P1** блокирует продакшн ·
**P2** важно · **P3** можно жить.

**Итог аудита v0.10.2 → v0.11.x:** из четырёх P0 закрыты **A3** (реконнект) и **A4** (layout);
**A1/A2** (конвертер планов) осознанно отложены до планов в новом формате (DXF + блоки с
атрибутами) — чинить нынешний алгоритм бессмысленно, см. ниже. Закрыты также D5, D9, D10, D19,
D20, D21, D22. В v0.11.1–0.11.2 добавлено полламповое управление на плане комнаты и снят
латентный баг «план не перетащить пальцем по зоне» (тап/перетаскивание — по сдвигу пальца).
**Проверено на объекте 2026-07-15:** перетаскивание, полламповый тап, реконнект.

---

## P0

> Правки компоновать **по месту** (один патч на файл/подсистему), а не по одному пункту.

### A1. Привязка имени к значку — ГАДАНИЕ. Решение: перейти на DXF (ждём планы)
> **Статус: осознанно отложено (2026-07-14).** Ждём новые планы от пользователя, где
> устройство — **блок AutoCAD с атрибутом имени** (`l_x_x_x`). Тогда имя берётся ИЗ САМОГО
> ОБЪЕКТА, и задача «сопоставить подпись значку» исчезает как класс. Чинить нынешний
> алгоритм смысла нет — см. ниже, почему он нечиним в принципе.

**Замер на реальном `3_etazh.svg` (v0.10.2):**

| Что | Значение |
|---|---|
| расстояние от подписи до её значка | медиана **11.4** ед. |
| шаг между соседними светильниками | медиана 31, **минимум 11.5** |
| подписей с отрывом от 2-го кандидата < 5 ед. | **51 из 166** |
| подписей с нулевым отрывом (0.0–0.1) | **5** |
| подписей, не взаимно-ближайших со своим значком | **31** |

Подпись удалена от своего светильника **ровно настолько же, насколько светильники удалены
друг от друга**. У `l_1_14_4` расстояние до «своего» и до чужого значка совпадает до десятой
доли — выбор решает шум округления. Это не «алгоритм жадный, надо получше»: **информации для
однозначного ответа в чертеже нет**. Венгерский алгоритм, mutual-nearest и любая другая
математика будут гадать по тем же неразличимым данным.

Запасная гипотеза (CAD ставит подпись с постоянным отступом → искать «вектор как у всех»)
**не подтвердилась**: разброс векторов от `(-30, 0)` до `(+8, +8)`, подписи двигали вручную.

⚠ **Поэтому текущий `assets/floors/3_etazh.svg` почти наверняка уже содержит перепутанные
имена.** Считать его эталоном нельзя. Проверить на объекте (тапнуть лампу — посмотреть,
какая загорелась) стоит, но чинить — только новым конвейером.

**Корень: конвейер уничтожает то, что мы потом угадываем.**
```
DWG   лампа = блок, номер = его атрибут        ← связь ЕСТЬ, явная
 ↓ экспорт в PDF
PDF   анонимные кривые + текст отдельно        ← связь УНИЧТОЖЕНА
 ↓ Inkscape → SVG → plan_convert.py
      «какой текст ближе к какой кривой?»      ← ГАДАЕМ то, что сами выбросили
```

**Целевое решение (когда будут планы):** экспорт **DXF** (не PDF) + чтение через `ezdxf`.
Устройство приходит как `INSERT` с атрибутом → имя из объекта, сопоставлять нечего.
Побочно закрывает **A2**, проблему `stroke-width` и захардкоженный масштаб `0.16`
(в DXF координаты модельные, матриц CAD-экспорта нет).

**Страховка в любом случае:** конвертер должен **падать с ошибкой** на неоднозначности,
а не догадываться. Сейчас порог «подозрительно» = `0.8 × 60 = 48` ед. при фактическом
максимуме 31 — ветка **недостижима**, отчёт молчит всегда и печатает «ПРИВЯЗАНО: 166».

### A2. Не схлопнутая геометрия уезжает за пределы плана (`tools/plan_convert.py:537`)
> Закрывается вместе с A1 переходом на DXF. Оставлено в реестре, чтобы не потерять.

`transform` снимается со **слоя безусловно**, а координаты схлопываются только у сматченных
элементов. Значок без подписи, дубль имени, зона без `id`, **любой слой не из четырёх
известных** (двери, размеры, штриховки) — сохраняют относительные координаты и уезжают
на ~126×70 ед. Сейчас не проявилось только потому, что на чертеже ровно 166:166 и лишних
слоёв нет. Это везение, а не инвариант.

### ~~A3. WebSocket не переподключается~~ — ✅ ЗАКРЫТ (v0.11.0)
Реконнект с паузой 1с→30с, восстановление подписок (после разрыва их id мертвы), отклонение
висящих запросов, перечитывание состояний после восстановления связи, плашка «нет связи» +
гашение органов управления. `auth_invalid` реконнект не крутит.
**Где:** `js/ha-ws.js`, `js/app-state.js → handleConnectionStatus()`, `js/shell-ui.js`, `css/shell.css`.

<details><summary>Как было</summary>
Обработчик `close` только пишет в лог. При обрыве (рестарт HA, Wi-Fi, ночь) интерфейс
показывает **застывшую, убедительно выглядящую картинку**: зоны горят, датчики «работают»,
а тапы молча отваливаются (`send` реджектится). Лечится только F5 — диспетчер не догадается.
Для настенного экрана 24/7 это дефект №1. Плюс висящие `pending`-промисы при `close` не
отклоняются (зависшие `await` навсегда).

</details>

### ~~A4. Layout затирается целиком~~ — ✅ ЗАКРЫТ (v0.11.0, backend v0.2.0)
`asyncio.Lock` вокруг load→mutate→save; ревизия документа (`meta.rev`) + `base_rev` в `layout/save`
(конфликт → ошибка `layout_conflict`, а не тихая перезапись); точечные записи `layout/ui/update`
(тема) и `layout/devices/update` (только изменённые устройства). Смена темы больше не отправляет
координаты всех устройств.
**Проверено:** параллельные «расстановка + тема» → оба изменения выжили; 20 конкурентных записей → 20/20.

<details><summary>Как было</summary>
Фронт **всегда** шлёт `layout/save` со всем документом из своей вкладки; backend
(`storage.py:121`) пишет его поверх без слияния с сохранённым. В backend классический
read-modify-write **без `asyncio.Lock`** — параллельные сохранения теряют друг друга.

**Сценарий:** пусконаладчик расставил устройства с ноутбука; диспетчер на планшете со старой
вкладкой **переключает тему** (`shell-ui.js:92` шлёт весь layout) → расстановка стёрта. Тихо.

</details>

---

## P1 — блокирует реальный объект

### D1. Загружаем и слушаем «всё сразу»
`ArvidHaRegistry.loadAll()` тянет **все** `get_states` + 4 реестра, а подписка —
глобальный `subscribe_events(state_changed)` на весь HA. На объекте ~4400 устройств это
стоп-фактор (трафик, память, лишние события).

**Целевое** (DESIGN реш. 13): `subscribe_entities` только на сущности текущего сегмента;
их объявляет сам SVG через `data-entity`. Кешировать только статику (SVG/структуру).

**Где:** `js/ha-registry.js → loadAll()`, `js/ha-ws.js → subscribeStateChanged()`, `js/app-state.js`.
**Связано с:** D2.

### ~~D2. `data-entity` из SVG не читается~~ — ✅ ЗАКРЫТ (v0.10.0)
Устройства читаются из самого плана (`data-entity`), и на этаже, и в комнате. Планы из CAD
готовит `tools/plan_convert.py` (docs/SVG_PLAN_SPEC.md).

Два пути координат сосуществуют: **расстановка** (`layout.devices` → `.device-marker`) для
частных объектов и **план** (`data-entity` → `.device-node`) для продакшна.

**Где:** `js/floor-page.js → collectPlanDevices()/updatePlanDeviceStates()`, то же в `room-page.js`.

---

## P2 — важно для функционала

### ~~D3. Логика режимов карты (Фаза 2)~~ — ✅ ЗАКРЫТ (v0.8.0)
Три режима работают. Освещение — оранжевый по свету; Присутствие — зелёный по `motionActive`
(свет игнорируется); Диагностика — серые линии плана, красный пульс на «не на связи», янтарный
на аномалиях. Источник диагностики — **ядро DALI** (`health_data`), своей логики offline нет.

**Где:** `js/health.js`, `js/floor-page.js → setMapMode()/applyZoneStateClasses()`, `css/floor.css`.

### ~~D4. Селект режима карты не виден на мобильном~~ — ✅ ЗАКРЫТ (v0.8.0)
Решён конструктивно, а не медиазапросом: `<select>` из `.topbar-actions` заменён
таб-переключателем `.map-mode-switch` — плавающим оверлеем над планом (как `.plan-toolbar`).
Виден одинаково на ПК и телефоне, мобильную сетку не трогает (высота плана считается от ширины
`.svg-stage`). Заодно соответствует формулировке ТЗ «переключатель-табы над картой».

### ~~D5. Состав комнаты не пересобирается при изменении набора устройств~~ — ✅ ЗАКРЫТ (v0.11.0)
Реестр слушает `entity/device/area/floor_registry_updated` и `new_state = null`, перечитывает
реестры (дебаунс 1.5 с) и зовёт `addCompositionHandler` → страницы перерисовываются.
В режиме редактирования перерисовка откладывается до выхода (иначе снесло бы маркер под курсором).

**Где:** `js/ha-registry.js`, `js/floor-page.js → handleCompositionChanged()`, то же в `room-page.js`.

### ~~D6. Честная обработка `unavailable` / offline в комнате (DESIGN реш. 17)~~ — ✅ ЗАКРЫТ (v0.9.0)
Маркер недоступного устройства приглушён + красное кольцо; аномалия — янтарное. Плашка
«N устр. не на связи» в карточках; строки датчиков/панелей показывают «не на связи» вместо
устаревших показаний; управление светом блокируется, когда живых ламп в комнате не осталось.

Источник — снимок ядра (`health_subscribe`), своей логики `unavailable` не ведём.
Сопоставление — **по `device_id`** (пара `ms_`/`il_` = одно устройство, один маркер, две записи).

**Где:** `js/room-page.js → getHealthFor()/renderDeviceMarkers()/buildOfflineNote()`, `css/room.css`.

### D7. Индикатор «ожидание» при действии (DESIGN реш. 12)
Показываем реальное состояние без оптимистики — но и без индикации, что команда ушла.
Нужен спиннер/состояние «ожидание» на контроле до прихода `state_changed`.

### D8. CCT (цветовая температура) для ламп типа 0102
В скоупе v1 (DESIGN реш. 7), не реализовано. Сейчас только вкл/выкл + яркость.

---

## P3 — можно жить

### ~~D9. Мёртвый код Quick View~~ — ✅ ЗАКРЫТ (v0.10.2, удалён)
Решение: **удалить**. Вернём при нужде — с наработанным функционалом, а не воскрешением
старого кода. Вместе с ним ушёл спрятанный внутри нарушитель инварианта «свет только через
группы» (`refreshRoomQuickView` собирал лампы поиском в обход HA-группы).

### ~~D10. `renderFloorDashboard()` перестраивает DOM сводки~~ — ✅ ЗАКРЫТ (v0.11.0)
Каркас сводки строится при смене этажа, по событиям обновляются только числа.

### D11. Расписание вне скоупа
`schedule-ui.js` + `schedule.css` + popup в `index.html` унаследованы от старого интерфейса,
работают, но в скоуп v1 не входят (DESIGN). Решить: оставить, спрятать или удалить.

### D12. «Живой» индикатор панелей (DESIGN реш. 9, 18)
Сейчас показывается только текст последнего события. Требуется мигание/подсветка при нажатии
и лента событий.

### ~~D13. Светильники на общем плане при зуме~~ — ✅ ЗАКРЫТ (v0.10.0, Фаза 3.5)
Порог `DEVICE_ZOOM_THRESHOLD = 2.2`: издалека помещение показывает агрегат (зона), при зуме
зона гаснет и солируют устройства — проблемные пульсируют красным точечно.

Элементы уже в DOM (пришли вместе с SVG), поэтому переключение — класс `is-zoomed` на
контейнере, а не перерисовка. **Осталось:** замер памяти/быстродействия на 166 устройствах
(пункт ТЗ) и подбор порога по факту — сейчас 2.2 «на глаз».

**Где:** `js/floor-page.js → applyZoomLevel()`, `js/svg-utils.js → onZoom`, `css/floor.css`.

### D14. `arvid_dali_center` read-only WS — используется частично
Вызываем здоровье устройств: основной путь — push `health_subscribe` (v0.8.2, ядро ≥ v1.1.1),
фолбэк — `health_data` (старое ядро). Идёт в режим «Диагностика» и «Предупреждения».
Остальное пока не трогаем: состав DALI-группы (`groups`), живой поток событий панелей
(`events_subscribe` → D12), энергия (`energy_data`/`energy_live` — вне скоупа v1).
См. [WEB_INTERFACE_API.md](WEB_INTERFACE_API.md).

### ~~D19. `floor-page` не фильтрует чужие события~~ — ✅ ЗАКРЫТ (v0.11.0)
Фильтр `isFloorEntityId()` (сущности помещений этажа + устройства плана + группы света + режимы),
индекс area в реестре, один пересчёт статистики за кадр.

<details><summary>Как было</summary>
`ArvidFloorPage.handleStateChanged()` реагирует на **любое** событие HA (подписка глобальная) и в
каждом кадре пересчитывает `getRoomStats()` для всех зон, а каждый вызов — полный резолв area по
всем сущностям (`registry.getEntitiesForArea` = `states.filter`). На тестовом стенде незаметно,
на объекте (~4400 сущностей × N зон в кадр) станет дорого.

В комнате это уже закрыто кешем (v0.8.1, Fix D). На этаже нужен аналог: индекс `area_id → сущности`
в `ArvidHaRegistry`, инвалидация по изменению реестра.

</details>

### ~~D18. Здоровье опрашивается поллингом, push-канала нет~~ — ✅ ЗАКРЫТ (v0.8.2)
Ядро v1.1.1 дало `health_subscribe` (не-admin). Перешли на push; поллинг остался фолбэком для
старого ядра.

> **Здесь была фактическая ошибка.** Мы писали: «ядро не отдаст ошибку раньше грейса
> `interval_min` (5 мин), поэтому опрашивать чаще бессмысленно». Это верно только для **устройств**.
> У `gw_offline` грейса нет — оценщик ставит его через ~1.5 с после сигнала связи (`_EVAL_DEBOUNCE`).
> Самое срочное событие мониторинга, «шлюз упал», поллинг задерживал до 300 с.

Дополнительно: `health_data` **форсирует полный пересчёт** здоровья на каждый запрос (O(N) по всем
устройствам + резолв трёх реестров на каждое, в петле HA). Подписка пересчёт не вызывает.
Поэтому поллить `health_data` нельзя иначе как фолбэком. (Долг ядра — HD1.)

### D15. Тонкая модель прав
`layout/save` требует `require_admin`, то есть расстановку может делать только админ-токен.
Уровни Viewer/Operator/Editor из WEB_INTERFACE_API §6 не выражены.

### D16. Реальные планы объекта (Фаза 3)
`default-floor.svg` и `rooms/ofis.svg` — нарисованы нами. Нужны настоящие планы
(чистые стены, полигоны-обводки по фактической геометрии, не прямоугольники).

### ~~D20. Квадратичные резолвы в реестре~~ — ✅ ЗАКРЫТ (v0.11.0)
Индексы `area_id → сущности` и `device_id → сущности`; обновление состояния O(1); удаление
сущности обрабатывается. **Замер:** 301 мс → 5 мс (4400 сущностей, 200 событий × 3 зоны).
⚠ **Замер памяти на 166 устройствах (пункт ТЗ) — всё ещё НЕ делали**, теперь он осмыслен.

<details><summary>Как было</summary>
`getEntitiesForArea()` и `getEntitiesForDevice()` — полный `states.filter` по всему реестру,
а зовутся **в циклах**:
- этаж слушает **все** события HA (D19) и на каждое пересчитывает `getRoomStats` для каждой
  зоны **дважды** за кадр (`applyZoneStateClasses` + `renderFloorDashboard`), а внутри —
  `entitiesForArea`. На объекте: ~4400 сущностей × 2 прохода × N зон на **каждое** показание люкса;
- `collapseToUnits` / `getSensorReadings` зовут `getEntitiesForDevice` на каждый датчик →
  **O(N²)**. Тяжелее всего в редакторе расстановки (список строится по всем устройствам HA).

**Нужно:** индексы `area_id → сущности` и `device_id → сущности` в `ArvidHaRegistry` с
инвалидацией по изменению реестра. **Замер памяти на 166 устройствах (пункт ТЗ) делать
ПОСЛЕ индексов** — иначе он измерит уже известную проблему.
Также `updateStateFromEvent()` делает `states.findIndex` (O(N)) на каждое событие, а
удалённые сущности (`new_state = null`) из `stateById` не убираются.

</details>

**Связано с:** D1 (остаётся: `subscribe_entities` по сегменту вместо `loadAll`).

### ~~D21. Ошибка маршрута уничтожает вёрстку~~ — ✅ ЗАКРЫТ (v0.11.0)
Оверлей с кнопкой «Повторить» вместо `innerHTML = "Ошибка…"`.

<details><summary>Как было</summary>
`spa-router.js → handleRouteError()` делает `activeView.innerHTML = "Ошибка маршрута…"`,
а бутстрап в `index.html` — `document.body.innerHTML = …`. Один неудачный старт при
недоступном HA (он же перезагружается) — и страница разрушена необратимо, лечится только F5.
</details>

### ~~D22. `LOG_LEVEL: "debug"` в продакшене~~ — ✅ ЗАКРЫТ (v0.11.0)
В проде `warn`; отладка — `localStorage.setItem("arvid.logLevel", "debug")`.

<details><summary>Как было</summary>
`js/config.js` логирует **каждое** событие HA, включая поток люксов — в самом горячем пути.
</details>

### D23. Фиктивные «Режимы работы» — P3, решено оставить
`getDisplayModes()` подставляет «Авто»/«Ручной» с несуществующими `input_boolean.*`, если в
layout нет режимов (а по умолчанию `modes: []`). При нажатии кнопка **только меняет свой цвет**
и пишет `warn`. Осознанная заглушка до реализации режимов здания. ⚠ Пока она есть — это UI,
который врёт о состоянии здания (против принципа «проблемы должны быть видны»).

### D24. Мелочи из аудита v0.10.2 — P3
- **Дубли портретных медиазапросов в `room.css`** (два блока `max-width:760px and portrait`):
  правки в первом молча не работают, побеждает второй. Мина под «святой» мобильной вёрсткой.
- `plan_convert.py`: `stroke-width` читается только из `style` (презентационный атрибут
  выживает в координатах CAD), масштаб толщины — **захардкоженная константа `0.16`** вместо
  значения из матрицы; `viewBox` принудительно обнуляет min-x/min-y (сдвиг плана, если в
  исходнике было смещение) и падает на запятых-разделителях; `rotate(a, cx, cy)` теряет центр
  вращения, `skewX/skewY` молча игнорируются; нет `makedirs` для выходного каталога.
- `plan_rooms.py`: устройства отбираются по **bbox** зоны, а не по контуру. Безопасно, пока
  зоны — прямые `rect`; **станет багом при переходе на зоны по фактической геометрии** (Фаза 3).
- `storage.py`: `layout["meta"]` не-словарь (клиент прислал `{"meta": null}`) → `TypeError`;
  схема `vol` не ограничивает размер и незнакомые ключи layout.
- Поля `building.default_floor_id`, `ui.*_panel_collapsed`, `meta` в сторе **не читаются**
  фронтом; в `ARCHITECTURE.md §5` в структуре стора пропущен `modes` (он есть и используется).
- Не-админ не может сохранить **даже тему** (`layout/save` требует `require_admin`), ошибка
  уходит только в консоль — пользователь ничего не видит. Связано с D15.

### D17. Фильтр по помещению в редакторе
Отложен осознанно (v0.4.0): список показывает все устройства HA + поиск. Когда area будут
заданы повсеместно, вернуть фильтр «только это помещение» как опцию.

---

## Задачи на стороне Home Assistant (не код)

| # | Что | Зачем |
|---|---|---|
| H1 | Создать группу `light.3_etazh` | «Выключить свет этажа» работает через группу; сейчас фолбэк + `warn` |
| H2 | Создать группу `light.all` | «Включить/выключить весь свет»; сейчас фолбэк на все `light.*` |
| H3 | Задать area устройствам (`danilka_*`, `andr_*`, датчики, панели) | Иначе они не входят в состав комнаты и в группу света (видны как «непривязанные») |
| H4 | Удалить лишнюю area `3_etazh` («3 этаж») | Создана как помещение по ошибке, висит в списке комнат без этажа |
| H5 | Почистить пробные area/floor (`office_alpha`, `meeting_beta`, `test_floor_a/b`) | Мусор в списках этажей и комнат |

## Задачи на стороне ядра DALI (`arvid-ha-dali-center`)

Мы туда не пишем — только просим. Правку делает пользователь.

### ~~H6. `health_data` → добавить `area_id`, `floor_id`, `entity_id` в записи `active`~~ — ✅ СДЕЛАНО ядром (v1.1.1)
Записи `active` несут `area_id`, `floor_id`, `entity_id` и **`device_id`** (сверх запроса).
Имена `area`/`floor` остались. Заодно ядро добавило `health_subscribe` (закрыл D18).

Что важно помнить о контракте (ядро, `docs/HEALTH.md §Адресация`):
- Поля есть у **каждой** записи; у `gw_offline` они `null`, кроме `device_id` шлюза.
- **Единственный случай отсутствия полей:** первые ≤15 с после рестарта HA, пока оценщик не сделал
  первый проход — `active` отдаётся из персиста в старой форме. Поэтому **фолбэк резолва по имени
  помещения оставлен** в `ArvidHealth.resolveAreaId()` и удалять его нельзя.
- Пара движение/люкс — одно устройство, две записи, общий `device_id` (см. D6).

**Реализовано:** v0.8.2, `js/health.js` (`byAreaId` / `byDeviceId` / `byEntityId`).
