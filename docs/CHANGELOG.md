# Changelog — ARVID Visual Interface

## v0.8.0 — 2026-07-10 — режимы карты (Фаза 2), здоровье от ядра DALI

Закрывает долги **D3** (логика режимов) и **D4** (селектор не виден на мобильном),
частично **D14** (read-only WS ядра) и **D6** (обработка offline на уровне этажа).

### Добавлено

- **Три режима карты** (селектор был с v0.6.1, логики не было):
  - **Освещение** — как раньше: оранжевый по включённому свету.
  - **Присутствие** — свет игнорируется, зелёным горят зоны с движением.
  - **Диагностика** — линии плана гаснут до серого, **красным пульсируют** зоны с устройствами
    не на связи, **янтарным** — зоны с аномалиями (залипшее движение, «мёртвый» люкс).
- **`js/health.js` (`ArvidHealth`)** — клиент здоровья устройств. Данные берём у ядра DALI
  (`arvid_dali_center/health_data`, read-only, без `require_admin`), **своей логики offline не ведём**.
- **Слот «Предупреждения»** наполнен: упавшие шлюзы, помещения с offline/аномалиями,
  счётчик записей без помещения. Раньше там была заглушка «Предупреждений нет».
- `ArvidDeviceUi.isScoped()` — общий фильтр скоупа (был приватным дублем в `room-page.js`).

### Изменено

- **Переключатель режимов** из `<select>` в топбаре стал **таб-переключателем над картой**
  (`.map-mode-switch`, оверлей как `.plan-toolbar`). На телефоне `.topbar-actions` скрыт —
  раньше режим там был недоступен; теперь контрол одинаков на ПК и телефоне (**D4**).
- `applyRoomLightHighlight()` → **`applyZoneStateClasses()`**: вешает на зону сразу все классы
  состояния (`has-light-on` / `has-motion` / `has-offline` / `has-anomaly`), а видимый слой
  выбирает CSS по `data-map-mode` на `.svg-stage`. Смена режима = один атрибут, без пересчёта;
  инвариант «DOM не перестраивается по `state_changed`» сохранён. Заодно `getRoomStats()`
  вызывается один раз на зону вместо одного на каждое состояние.
- `getRoomStats()` отдаёт `offlineCount` и `anomalyCount` (из снимка health).

### Исправлено

- **Fix A — движение по всем датчикам помещения.** `motionActive` считался по **первому**
  найденному датчику (`entities.find`), поэтому в комнате с несколькими датчиками сработка
  на втором и далее игнорировалась — и в подсветке зон, и в сводке «Движение».
  Теперь `some()` по всем датчикам движения помещения.

### Решения (важно для понимания)

- **Диагностику не считаем сами.** Ядро уже ведёт таксономию ошибок (`HEALTH.md`): `lamp_offline`,
  `lamp_unknown`, `sensor_unknown`, `panel_unknown` → «не на связи» (красный пульс);
  `motion_stuck`, `motion_idle`, `lux_stale` → «аномалия» (янтарный); `gw_offline` → строка в
  «Предупреждениях» (шлюз к помещению не привязан).
- **Частота опроса.** У health нет push-канала, только снимок. Ядро не считает устройство
  сломанным первые `grace = interval_min` (**5 минут**) — защита от транзиентов при рестарте.
  Значит опрашивать чаще бессмысленно: в фоне **300 с**, в режиме «Диагностика» **30 с**
  (чтобы починенная зона гасла быстро), при скрытой вкладке — пауза, при возврате — свежий снимок.
- **Серость карты** сделана цветом линий плана (`--muted`), а **не** `filter: grayscale()`:
  фильтр обесцветил бы и сам красный пульс.
- **Деградация без ядра.** `unknown_command` → модуль выключается, поллинг останавливается,
  «Предупреждения» показывают «Данные о здоровье недоступны», режимы Освещение/Присутствие
  работают как обычно. Обрыв WS — временная ошибка, снимок сохраняется, поллинг живёт.

### Известное ограничение

`health_data` отдаёт **имя** помещения (`area.name`), а не `area_id`. Зоны плана живут по
`area_id`, поэтому имя резолвится через реестр HA; **дубли имён** сопоставить нельзя (уходят в
счётчик «Без помещения» + `warn` в лог). Запрос к ядру — добавить `area_id`/`floor_id`/`entity_id`
в записи (**H6** в [DEBT.md](DEBT.md)); код подхватит поле без правок.

## v0.7.0 — 2026-07-08 — состав комнаты = HA-area (истина), непривязанные помечены

### Изменено (важно)

- **Состав комнаты теперь определяется только Home Assistant** (`entity/device.area_id`),
  а не объединением «HA-area ∪ размещённые на плане» (как было с v0.4.0).
  Причина: объединение делало состав непрозрачным — в «Офисе» показывалось 9 ламп,
  хотя в HA к нему привязана только часть, остальные попали туда через расстановку на плане.
  По составу считаются карточки, счётчики «N/N включено» и статистика комнаты.
- **Маркеры на плане** рисуются по нашему layout (у кого есть координаты) — как и раньше.
- **Непривязанные помечаются:** устройство стоит на плане комнаты, но в HA к ней не привязано →
  маркер приглушён и обесцвечен (`.device-marker.is-unassigned`), а в комнате появляется плашка
  «N устр. на плане не привязано к помещению в HA».

Новое в API: `ARVID_APP.entitiesForArea()` (состав), `placedEntitiesForRoom()` (маркеры),
`isUnassignedInRoom()`. `entitiesForRoom()` (объединение) остался только как фильтр
«свои события» для подписки.

## v0.6.1 — 2026-07-08 — light.all, селектор режимов карты

### Изменено

- **«Включить/выключить весь свет»** — через HA-группу объекта **`light.all`**
  (`setAllLights`). Фолбэк на все `light.*` из реестра с предупреждением, если группы нет.
  Формула групп теперь полная: комната `light.<area_id>` · этаж `light.<floor_id>` · объект `light.all`.

### Добавлено

- **Селектор режима карты** в топбаре, справа от селекта этажа (`[data-map-mode]`):
  Освещение / Присутствие / Диагностика. Пока только фиксирует выбор (`setMapMode`)
  и выставляет `data-map-mode` на контейнере плана — логика подсветки будет в Фазе 2.
  На мобильном `.topbar-actions` скрыт, поэтому селект там не виден (решим с Фазой 2).

## v0.6.0 — 2026-07-08 — HA-группы света, конец перерисовки UI, docs/

### Исправлено

- **Fix A — мигание кнопок и слайдера при потоке показаний.** `handleStateChanged` вызывал
  полную перестройку DOM (`renderControls` делал `innerHTML=""`), а на этаже ещё и
  `bindRoomZones()` перевешивал все обработчики зон — на **каждое** `state_changed`.
  При включении света поток от люкс-датчика давал десятки перестроек в секунду.
  Разделены **render** (построение) и **update** (только значения):
  - фильтр чужих сущностей + коалесценция событий в один кадр (`requestAnimationFrame`);
  - точечное обновление по якорям `data-entity`, `data-light-count`, `data-sensor-anchor`,
    `data-panel-entity`, `data-mode-entity`;
  - слайдер яркости не трогается, пока пользователь его держит или он в фокусе;
  - `bindRoomZones()` больше не вызывается по `state_changed`.

### Изменено

- **Управление светом — через HA-группы, а не поиск ламп** (детерминированно):
  комната → `light.<area_id>` (например `light.ofis`), этаж → `light.<floor_id>`
  (например `light.3_etazh`). Хелпер `ARVID_APP.lightGroupState()`. Если группы нет —
  фолбэк на сборку ламп + предупреждение в лог. Групповая сущность исключена из счётчиков
  «N/N включено» и из списка ламп-членов. Затронуты: тап по зоне, «Вся комната» в карточке
  освещения, «Выключить свет этажа».
- **Документация переехала в `docs/`** (ARCHITECTURE, DESIGN, DEPLOY, ROADMAP,
  WEB_INTERFACE_API, CHANGELOG). `CLAUDE.md` остался в корне.

### Добавлено

- **docs/REQUIREMENTS.md** — живой чек-лист «требование → как реализовано у нас» с указанием
  файлов и методов (в т.ч. подробно: зум/пан, полигоны помещений, подсветка).

### Удалено

- Фиксированная кнопка «Выключить этаж» поверх карты (v0.5.0) — дублировала быстрое действие
  в правой панели.

## v0.5.0 — 2026-07-08 — модель взаимодействия карты (Фаза 1 ROADMAP)

Прямое управление комнатами с плана этажа + кнопка гашения этажа — ядро DoD заказчика.

### Изменено

- **Короткий тап по зоне комнаты = вкл/выкл света** всего помещения (`toggleRoomLights`).
- **Двойной тап по зоне = переход на план комнаты** (различение по таймауту 250 мс).
- **Quick View (мини-карточка по клику) отключён** — вызов убран из обработчика зон,
  код методов оставлен (вернём при необходимости), по решению A в ROADMAP.
- **«Выключить этаж» переписан на фильтр по этажу** (`getFloorLightEntityIds`): собирает
  `light.*` всех комнат этажа через `entitiesForRoom` (HA area ∪ размещённые). Не требует
  заранее заданной групповой сущности этажа; заранее сформированные группы света попадают сами.

### Добавлено

- **Фиксированная кнопка «Выключить этаж»** поверх карты, вне зум-зоны (ТЗ Task 3).
- `touch-action: manipulation` на зонах и кнопке — двойной тап не зумит страницу на телефоне.

## v0.4.1 — 2026-07-08 — иконки в редакторе, единый датчик, клик-размещение

### Исправлено

- **Fix A — пустые чёрные иконки в редакторе.** Рамка выделения (`rect.device-marker-selection`)
  добавлялась поверх иконки, а CSS-селектор `.device-marker rect:not(.device-marker-hit)`
  заливал её тёмным (специфичность выше `.device-marker-selection`). Исключил selection
  из заливки — иконки в редакторе теперь как на обычной странице.
- **Fix B — датчики движения (`ms_`) не появлялись в списке.** У DALI-датчиков движения
  `device_class` пустой, а `isMotion` ждал его. Добавил распознавание по префиксу `ms_`
  (и `il_` для освещённости). Причина «доступны только il_» устранена.

### Изменено

- **Единый датчик движения+освещённости.** Пара `sensor.ms_*` + `sensor.il_*` — одно
  физическое устройство (общий `device_id`), теперь это **одна точка** на плане и одна
  строка в списках (kind `sensor`, иконка-радар). Показания — оба сразу: движение · lx
  (в карточке комнаты и в попапе маркера). Схлопывание — `collapseToUnits` по `device_id`,
  якорь пары — сущность движения.
- **Fix C — размещение щелчком/тапом.** Раньше клик по плану ставил устройство только на
  телефоне (на ПК — лишь «В центр плана»). Теперь: выбрал устройство → щёлкнул/тапнул по
  месту → появилось (и на ПК, и на телефоне). Клик отличается от панорамирования по сдвигу.

### Новое в API модулей

- `ha-registry`: `getDeviceId(entityId)`, `getEntitiesForDevice(deviceId)` — для связи пары ms_/il_.
- `device-ui`: `isSensor`, единый `markerKind === "sensor"`; `room-page`: `collapseToUnits`,
  `getSensorReadings`, `formatSensorReadings`.

## v0.4.0 — 2026-07-08 — подсветка света, поиск устройств, реалистичные планы, документация

### Добавлено

- **Подсветка зон комнат** (раньше стилей `.room-zone` не было вообще):
  hover и активная зона — акцентная заливка; **комната с включённым светом —
  прозрачный оранжевый** (`.room-zone.has-light-on`, обновляется на каждый `state_changed`).
- **Поиск устройств по названию** в режиме редактирования (поле над списком; фильтрует
  строки локально, не теряя фокус). Ищет по friendly-имени и entity_id.
- **Привязка «устройство → комната» через объединение** HA area ∪ размещённые нами
  (`ARVID_APP.entitiesForRoom`): свет комнаты и подсветка работают даже когда area
  устройствам не заданы (частные объекты).
- **ARCHITECTURE.md** — карта проекта: слои, модули и зависимости, потоки данных,
  backend, внешние зависимости, инварианты, техдолг.

### Изменено

- **Редактор показывает ВСЕ устройства скоупа HA**, а не только area комнаты
  (`getAllScopedEntities`). Причина: на офисе area устройствам не назначены, а расставить
  нужно любое. Фильтр по помещению (area) отложен как идея (нужен, когда area будут заданы).
- Чипы-фильтры по типу (Все/Свет/Датчики/Панели) сохранены.

### Планы (SVG)

- Удалены пробные планы: `floors/test_floor_a|b.svg`, `rooms/office_alpha|meeting_beta|
  hall_gamma|lounge_delta|service_epsilon.svg`.
- `default-floor.svg` — реалистичный этаж: горизонтальный коридор, кабинеты сверху/снизу;
  активна зона «Офис» (`data-room-id="ofis"`), остальные — неактивные.
- `rooms/ofis.svg` — план кабинета «Офис», пропорции ~2:1 (вытянут сверху вниз), пустой.
- `default-room.svg` — обновлён на чистый прямоугольник-fallback.

### Требует действия на HA (иначе зона офиса не кликается)

Привязать area **«Офис»** к этажу: HA → Settings → Areas & Zones → Офис → Floor.
Сейчас `ofis.floor_id = None`, поэтому зона на плане помечается «не на этом этаже».
Опционально: удалить пробные area (`office_alpha` и др.) и этажи `test_floor_*`.

## v0.3.0 — 2026-07-08 — собственный backend (домен visual_interface)

Форк отвязан от общего стора старого интерфейса. Своя HA-интеграция, свой чистый стор.

### Backend (новая интеграция, v0.1.0)

- `custom_components/visual_interface/` — форк прежнего `web_interface`:
  домен `visual_interface`, `STORAGE_KEY = "visual_interface.layout"` (отдельный стор),
  класс `VisualInterfaceStorage`, WS-команды `visual_interface/*`. Поведение 1:1
  (get/ping без прав, save/room/device под `require_admin`).
- Дефолтный layout почищен: убраны неиспользуемые `room_cards` и `ui.room_badges`
  (были про temperature/motion-бейджи старого интерфейса).
- Старый `custom_components/web_interface/` удалён из форка (живёт в мастер-проекте).

### Frontend

- `floorplan-storage.js`: все 5 команд переведены на `visual_interface/*`.
- Версия фронта → v0.3.0.

### Требует разового действия на HA

Добавить `visual_interface:` в `configuration.yaml` и перезапустить HA (config_flow нет).
Стор чистый — расстановку устройств в офисе нужно сделать заново (осознанный выбор:
уходим от замусоренного `web_interface.layout`).

## v0.2.1 — 2026-07-08 — фикс «пустых» маркеров, чистка топбара

### Исправлено

- **Пустые неубираемые иконки на плане комнаты.** Причина: на плане (обычный режим)
  маркеры рисовались для ВСЕХ сущностей помещения (`getRoomEntities`), включая старые
  записи layout вне скоупа (климат/шторы/сцены) — им общий стор `web_interface.layout`
  проставил координаты, а иконок для них уже нет → рисовался пустой кружок. В режиме
  редактирования список берётся из скоупа, поэтому такие устройства нельзя было выбрать
  и «убрать с плана». Фикс: единый фильтр скоупа `getScopedEntities()` и для плана, и для
  редактора — вне-скоупные устройства не рисуются вообще (`getEditableEntities` → `getScopedEntities`).

### Удалено

- Кнопка «Автоматизации» в топбаре главной (была статичной заглушкой без обработчика).

> Первопричину — общий замусоренный layout-стор со старым интерфейсом — закрывает
> переход на собственный backend (в работе, см. CLAUDE.md «Дальнейший путь»).

## v0.2.0 — 2026-07-07 — редактирование внутри помещения, единые иконки

Правки контракта (DESIGN.md, решение 20): редактор — НЕ отдельная страница, а режим
редактирования внутри помещения; функциональность упрощена до списка устройств с фильтрами.

### Изменено

- **Режим редактирования в комнате** (`room-page.js`): кнопка «Редактор» в шапке комнаты
  включает режим (кнопка становится «Готово»). Вместо панелей управления показывается
  карточка «Расстановка устройств»: фильтры (Все/Свет/Датчики/Панели), список устройств
  помещения (иконка + имя + entity_id + статус «на плане/не размещено»), подсказка,
  действия «В центр плана»/«Убрать с плана», «Сохранить разметку», строка статуса.
- **Перенос маркеров**: drag на компьютере, тап по плану на телефоне (механика прежнего
  редактора перенесена в комнату). Убранные с плана устройства в режиме редактирования
  видны полупрозрачными, выбранное устройство подсвечено рамкой.
- **Несохранённые изменения**: при выходе из режима — подтверждение
  (сохранить / отменить с перечиткой layout из HA).
- **Иконки в едином стиле** (панель-градиент + гравировка, viewBox 120): починен
  `light.svg` (у экспорта Illustrator был viewBox 14400 — иконка рендерилась пустой),
  добавлены `illuminance.svg` (солнце) и `panel.svg` (панель с ручкой и кнопками).
  Настройки формы/иконки маркера удалены — иконка назначается автоматически по типу.
- Заголовки вкладки: «ARVID Visual Interface» / «— Помещение».

### Удалено

- Страница редактора целиком: `editor-page.js`, `editor.css`, editor-view в `index.html`,
  кнопки «Редактор» в левой панели этажа, маршрут `view=editor` (fallback на floor),
  анимации переходов to-editor/from-editor, ссылки `#editorLeftPanel` в shell-ui,
  выбор типа маркера (круг/квадрат) и ручной выбор иконки.

### Версии

- Frontend: `v0.2.0`; backend не менялся.

## v0.1.0 — 2026-07-07 — форк web_interface, чистка под скоуп «свет»

Новый проект: форк `web_interface` v0.9.2 с сохранением мобильной вёрстки и SPA-архитектуры.
Скоуп зафиксирован в DESIGN.md: свет · движение/освещённость · кнопочные/поворотные панели.

### Перенесено из базы (без изменений)

- SPA: `index.html` (floor/room/editor), spa-router, View Transitions, ARVID_APP/ARVID_RUNTIME
- Вёрстка: shell/floor/room/editor/schedule CSS, мобильные аккордеоны, Quick View, popup расписания
- Backend `custom_components/web_interface` (хранилище layout, домен оставлен прежним —
  общий layout со старым интерфейсом, без перенастройки HA)
- Правила деплоя (DEPLOY.md, пути обновлены на `visual_interface`)
- Скиллы из `.claude copy` → `.claude/skills`
- Спека API ядра `WEB_INTERFACE_API.md` и контракт `DESIGN.md` (из arvid-web-interface)

### Удалено (вне скоупа)

- **Климат**: карточка климата в комнате, климат в Quick View и сводке этажа,
  `climate.*` из типов устройств, попап управления климатом, CSS `.climate-*`
- **Шторы**: карточка штор, `cover.*` из типов, иконка cover.svg
- **Датчики температуры/влажности/CO₂**: детекторы в device-ui, метрики Quick View,
  пороговые предупреждения этажа (temperature/humidity/co2), иконка temperature.svg
- **Сценарии**: карточка «Быстрые сценарии», `scene.*`/`script.*` из типов, CSS `.scenario-*`
- Иконки climate/cover/temperature/fan, устаревшие `room.html`/`editor.html`,
  локальные бэкапы `assets/Версия claude/`, неиспользуемый `supportsPopupControl`

### Добавлено (закладка нового скоупа)

- `device-ui.js`: `isPanelEvent()` + `panelEventText()` — панели как `event.*`
  (click/double/hold/hold_end/rotate + key_no), тип маркера `panel`
- Комната: read-only карточка «Панели» (последнее событие по каждой панели),
  попап панели по клику/долгому тапу на маркер
- Quick View: метрика «Освещённость» (lx) вместо климата/CO₂/влажности
- Сводка этажа: карточка «Движение» (помещения с активным движением, переход в комнату)
- Слот «Предупреждения» сохранён с заглушкой — наполнение сигналами скоупа спроектируем отдельно
- Редактор: типы устройств ограничены `light./sensor./event.`, иконка «Панель»
- CSS: цвет маркера `device-kind-panel` (комната + редактор)

### Версии

- Frontend: `v0.1.0` (`js/config.js`), новый отсчёт
- Backend: не менялся, остаётся на версии базы (0.9.x)
